;这一题的目标如正文所叙述的：
; One can impose a towerlike structure on this by ordering the variables
; and thus always converting any polynomial to a "canonical form"
; with the highest-priority variable dominant and the lower-priority variables
; buried in the coefficients.

; 在2-5-2中讨论了数的强制，而对于多项式的强制方式由此题呈现。
