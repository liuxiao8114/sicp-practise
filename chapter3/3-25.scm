(define (make-table)
  (define (insert key-list value table)
    (define (list-iter items)
      (if (null? items)

        (list (car items))
      )
    )
    (define (iter )
  )
  (define (lookup key-list)
    ()
  )

  (define (dispatch m)
    ()
  )

  dispatch
)

(define t (make-table))

(define get (t 'lookup))
(define put (t 'insert))
